---
import { SITE_TITLE, GITHUB_USER, GITHUB_SMARTCAR } from '../consts';

const today = new Date();
---

<footer>
	<p class="copy">&copy; {today.getFullYear()} {SITE_TITLE}</p>
	<p class="links">
		<a href={`https://github.com/${GITHUB_USER}`} target="_blank" rel="noopener noreferrer">GitHub</a>
		<span aria-hidden="true">·</span>
		<a href={GITHUB_SMARTCAR} target="_blank" rel="noopener noreferrer">Smartcar</a>
	</p>
</footer>
<style>
	footer {
		padding: 2rem 1rem 2.75rem;
		text-align: center;
		border-top: 1px solid rgba(57, 197, 187, 0.12);
		background: linear-gradient(180deg, transparent, rgba(7, 18, 20, 0.72));
	}

	.copy {
		margin: 0 0 0.4rem;
		color: rgba(148, 175, 178, 0.85);
		font-size: 0.82rem;
		letter-spacing: 0.06em;
	}

	.links {
		margin: 0;
		font-size: 0.82rem;
		letter-spacing: 0.08em;
		text-transform: uppercase;
	}

	.links a {
		color: rgba(57, 197, 187, 0.9);
	}

	.links a:hover {
		color: #fff;
	}

	.links span {
		margin: 0 0.45em;
		color: rgba(100, 130, 135, 0.8);
	}
</style>
